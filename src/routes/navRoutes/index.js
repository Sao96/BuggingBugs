const navRoutes = {
    landing: "/landing",
    login: "/login",
    register: "/register",
    settings: "/settings",
    dashboard: "/dashboard",
    ticketboard: "/ticketboard",
};

Object.freeze(navRoutes);

export { navRoutes };
