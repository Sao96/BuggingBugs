const navRoutes = {
    landing: "/landing",
    login: "/login",
    register: "/register",
    dashboard: "/dashboard",
    ticketboard: "/ticketboard",
};

Object.freeze(navRoutes);

export { navRoutes };
