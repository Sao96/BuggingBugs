/**
 * @function demoCleanup
 * Goes through DB after an hour when the demo session expires and deletes all
 * related data
 *
 * @param {*} uid
 */
async function demoCleanup(uid) {}

export { demoCleanup };
